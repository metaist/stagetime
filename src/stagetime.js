/**
  stagetime - Show or hide DOM elements based on the local time.
  @namespace stagetime
*/

/**
  @constant
  @memberof stagetime
  @name version
  @summary [Semantic Version](http://semver.org) of the package.
*/
export {version} from '../package.json';

// Core
export {init, DEFAULTS} from './core';
